# Gateway base URL (no trailing slash)
#
# Option A (direct): http://127.0.0.1:18789  (requires Gateway CORS for browser)
# Option B (dev proxy): /api  (vite dev server will proxy /api/* -> http://127.0.0.1:18789/*)
VITE_GATEWAY_URL=http://127.0.0.1:18789

# Bearer token (required)
VITE_GATEWAY_TOKEN=replace_me

# Optional sessionKey for tool calls (default: main)
VITE_GATEWAY_SESSION_KEY=main
