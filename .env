# Gateway base URL (no trailing slash)
#
# Use /api to proxy requests to local Gateway (CORS workaround for dev)
# For remote Gateway (Tailscale Funnel, production), use the actual URL
VITE_GATEWAY_URL=/api

# Authentication mode: "password" or "token"
VITE_AUTH_MODE=password

# Bearer token (used when VITE_AUTH_MODE=token)
# VITE_GATEWAY_TOKEN=replace_me

# NOTE: When VITE_AUTH_MODE=password, enter password in the login dialog
# (password is not stored in .env for security across multiple browsers)

# Optional sessionKey for tool calls (default: main)
VITE_GATEWAY_SESSION_KEY=main
